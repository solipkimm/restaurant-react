import { Card } from "react-bootstrap";

function NotFound() {
  return (
    <Card>
      <Card.Body>
        <Card.Text>No Restaurants Found</Card.Text>
      </Card.Body>
    </Card>
  );
}

export default NotFound;